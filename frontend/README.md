# DroneSeg Frontend

Next.js frontend for the DroneSeg map viewer and detection workflow.

## Local run without Docker

```bash
cd frontend
npm install
cp .env.local.example .env.local
npm run dev
```

The backend should be running at `http://localhost:8000`.
